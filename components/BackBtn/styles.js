import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  backBtn: {
    justifyContent: 'center',
    backgroundColor: '#fff',
    width: 30,
    height: 30,
    borderRadius: 15,
    margin: 15
  }
});
